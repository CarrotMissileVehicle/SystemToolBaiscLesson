\documentclass{article}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage[UTF8]{ctex}
\begin{document}
\title{Tool Report}
\author{周御基 --- 25020007191}
\maketitle
\section{Result}

我们在此给出模型使用的线性函数。设输入为 $x$，输出为 $y$，则
\begin{equation}
  y = w x + b,
  \label{eq:linear}
\end{equation}
其中 $w$ 是权重（weight），$b$ 是偏置（bias）。公式~\eqref{eq:linear} 表明
``输出是输入的线性变换''。

观测到的两组数据如表~\ref{tab:data} 所示。

\begin{table}[htbp]
  \centering
  \begin{tabular}{cc}
    \toprule
    $x$ & $y$ \\
    \midrule
    1 & 5 \\
    2 & 9 \\
    \bottomrule
  \end{tabular}
  \caption{示例数据}
  \label{tab:data}
\end{table}

表~\ref{tab:data} 中的数据说明了输入与输出之间的对应关系。

\end{document}
